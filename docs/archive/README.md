# Archive

这里存放旧执行线的归档资料。它们不再作为默认主控入口。

- `legacy-master/competition-master-execution-plan.md`：旧总控文档，已被 `docs/feishu-memory-copilot-implementation-plan.md` 取代。
- `legacy-day-docs/`：旧 `day1` 到 `day7` 的执行计划、handoff、截图和阶段说明。

使用规则：

- 新任务默认不要读取整个 archive。
- 只有当天日期计划明确需要旧能力时，才按需读取对应文件。
- archive 内容只作为 reference / fallback；当前代码和新主控计划优先。

