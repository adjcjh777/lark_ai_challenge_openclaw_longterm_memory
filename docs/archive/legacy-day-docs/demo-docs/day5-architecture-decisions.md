# Day5 Demo 架构决策文档

这份文档用于演示 Memory Engine 从飞书文档中抽取候选记忆。下面混合了架构决策和会议背景信息。

## 可抽取记忆

- 决定：生产部署必须加 --canary --region cn-shanghai，禁止直接全量发布。
- 决定：后端框架最终采用 FastAPI，不再评估 NestJS。
- 规则：数据存储采用 SQLite + Bitable 双层结构，本地 SQLite 是核心事实源。
- 规则：Benchmark 报告必须展示 Recall@1、MRR、证据覆盖率和平均延迟。
- 约束：飞书 Bot 权限未确认时必须使用 replay 和 dry-run 完成演示。

## 干扰信息

- 今天会议从 10:00 开始，投影仪第一次没有自动连接。
- 讨论过程中有人建议把截图放到最后一页。
- 白板上画了三种颜色的箭头，颜色只用于区分图层。
- 午饭前先整理录屏目录，避免文件名重复。
- 大家确认演示电脑电量充足。
- 评委可能会关注业务价值，但这句话不是可执行规则。
- 会议室空调温度偏低，后续可以换座位。
- 这份文档保留一些普通描述，用来测试抗干扰能力。
- 录屏前需要关闭无关通知。
- 文档标题先保持现状，提交前再由人工整理。
- 讲解时先说问题，再说系统如何解决。
- 这些背景信息不应该进入 Memory Engine。
- 复盘材料附上关键截图。
- 备份视频放到本地目录，不写入代码仓库。
- 如果页面加载慢，先展示 CLI 路径。
