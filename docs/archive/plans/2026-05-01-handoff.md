# 2026-05-01 Handoff

> **历史状态更新（2026-04-28）**：本文件对应的 2026-05-05 及以前任务已经完成，保留为历史计划/交接证据，不再作为后续执行入口。新的执行入口是 `docs/productization/full-copilot-next-execution-doc.md`，下一步优先 Phase A：Storage Migration + Audit Table。

覆盖日期：2026-05-01
下一步日期：2026-05-02
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 今天已经把 old -> new 冲突更新闭成版本链：同一 scope + normalized subject 的新候选确认后，新版本变成 active，旧版本变成 superseded。
2. `memory.explain_versions` 已实现，可以说明当前有效值、旧值为什么失效、覆盖关系和 evidence（证据）。
3. 默认 `memory.search` 仍只返回 active 当前答案；superseded / stale 只进入版本解释或 deep trace，不进入默认 Top K。
4. Candidate Review card、Version Chain card 和 Bitable dry-run 表字段已成型，但本阶段没有接真实飞书按钮回调，也没有把 Bitable 当 source of truth。
5. 历史后续的 2026-05-02 `memory.prefetch`、heartbeat reminder candidate 和 OpenClaw demo dry-run flow 已经完成；当前不要再按该日期计划继续执行，后续从 `docs/productization/full-copilot-next-execution-doc.md` 的产品化 Phase A 开始。

## 已完成总结

- `memory_engine/copilot/governance.py`：冲突检测从严格 `type + normalized_subject` 硬化为同 scope + 同 normalized subject 优先；确认冲突 candidate 时会把旧 active version 标成 superseded。
- `memory_engine/copilot/service.py`、`memory_engine/copilot/tools.py`：`memory.explain_versions` 已接入 service 和 tool handler。
- `memory_engine/copilot/retrieval.py`：沿用 active-only 默认搜索边界，新增单测证明旧值不再作为当前答案。
- `benchmarks/copilot_conflict_cases.json`：新增 10 条真实冲突表达，包含“刚才说错了”“统一改成”“以后别用这个”等口吻，并标注 `expected_action=confirm`、旧值 forbidden 和失败排查提示。
- `memory_engine/benchmark.py`：新增 `copilot_conflict` runner，输出 Conflict Update Accuracy（冲突更新准确率）、superseded leakage、stale leakage 和 evidence coverage。
- `memory_engine/feishu_cards.py`：新增 Candidate Review / Version Chain typed payload 和 dry-run card builder，只消费 Copilot service 输出，不直接改状态。
- `memory_engine/bitable_sync.py`：Bitable dry-run payload 扩为五类表：Memory Ledger、Memory Versions、Candidate Review、Benchmark Results、Reminder Candidates。
- `tests/test_copilot_governance.py`、`tests/test_copilot_tools.py`、`tests/test_copilot_benchmark.py`、`tests/test_feishu_interactive_cards.py`、`tests/test_bitable_sync.py`：覆盖冲突版本链、工具入口、benchmark、card payload 和 Bitable dry-run 字段。

## 验证结果

已运行：

```bash
python3 scripts/check_openclaw_version.py
git diff --check
python3 -m compileall memory_engine scripts
python3 -m unittest tests.test_copilot_governance
python3 -m unittest tests.test_copilot_tools
python3 -m unittest tests.test_copilot_benchmark
python3 -m unittest tests.test_feishu_interactive_cards tests.test_bitable_sync
python3 -m unittest tests.test_copilot_governance tests.test_copilot_benchmark
python3 -m unittest tests.test_copilot_governance tests.test_copilot_tools tests.test_feishu_interactive_cards tests.test_bitable_sync tests.test_copilot_benchmark
python3 -m memory_engine benchmark run benchmarks/copilot_conflict_cases.json
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
ollama ps
```

当前已确认：

- OpenClaw version OK: 2026.4.24。
- governance / tools / benchmark / card / bitable 专项单测通过。
- `copilot_conflict_cases.json`：10 条样例，case_pass_rate = 1.0，conflict_accuracy = 1.0，conflict_detected_rate = 1.0，superseded_chain_rate = 1.0，stale_leakage_rate = 0.0，superseded_leakage_rate = 0.0，evidence_coverage = 1.0。
- `day1_cases.json` fallback：10 条样例，case_pass_rate = 1.0，conflict_accuracy = 1.0，stale_leakage_rate = 0.0。
- `ollama ps` 显示无本项目模型驻留；今天没有运行真实 Cognee / Ollama embedding 验证。

最终提交前验证已完成。

## 仍未完成或仍有风险

- `memory.prefetch` 仍未实现，这是 2026-05-02 的主线；今天只保证 search 不泄漏旧值。
- Heartbeat reminder candidate 仍未实现，Reminder Candidates 表今天只是 dry-run 字段设计。
- Card button 回调没有接真实飞书群；今天只冻结 payload 和 dry-run card，不做生产写入。
- Bitable review surface 没有真实写入项目业务台账；今天只验证本地 dry-run payload 和 schema。
- 冲突检测现在按同 subject 优先，复杂多主题句子仍可能需要人工确认。

## 2026-05-02 接续建议

1. 历史建议原本指向 `docs/plans/2026-05-02-implementation-plan.md`；当前不要再执行该旧计划，先读 `docs/productization/full-copilot-next-execution-doc.md`。
2. 从 `memory_engine/copilot/service.py` 和 `memory_engine/copilot/tools.py` 实现 `memory.prefetch`。
3. 新增 `memory_engine/copilot/heartbeat.py`，先做 reminder candidate 和 dry-run，不真实推送。
4. 复用今天的 Candidate Review / Version Chain / Bitable dry-run payload，不要绕过 Copilot service 改状态。
5. 补 `tests/test_copilot_prefetch.py`、`tests/test_copilot_heartbeat.py` 和 OpenClaw examples。

## 我的补充任务

1. 已完成：`benchmarks/copilot_conflict_cases.json` 里放入 10 条可跑冲突样例，覆盖“刚才说错了”“统一改成”“以后统一改成”等表达。
2. 已完成：每条冲突样例都有 `expected_action`、旧值 `forbidden_value` 和 `failure_debug_hint`。
3. 已完成：`memory.explain_versions` 输出能说明当前有效值、旧值为什么失效、证据来自哪里。
4. 已完成：Candidate Review payload 能区分“待确认记忆”和“覆盖旧值”。
5. 已完成：Bitable Candidate Review 字段包含状态、主题、新值、旧值、证据、风险和操作建议。

本阶段不用做：

- 不用删除旧版本。
- 不用接真实群聊按钮回调。
- 不用把 Bitable 当 source of truth。
- 不用提前实现 2026-05-02 的完整 prefetch / heartbeat。
