# 2026-05-02 Handoff

> **历史状态更新（2026-04-28）**：本文件对应的 2026-05-05 及以前任务已经完成，保留为历史计划/交接证据，不再作为后续执行入口。新的执行入口是 `docs/productization/full-copilot-next-execution-doc.md`，下一步优先 Phase A：Storage Migration + Audit Table。

覆盖日期：2026-05-02
下一步日期：2026-05-03
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 今天已经把 `memory.prefetch` 接到 Copilot service / tools，Agent 任务前可以拿 compact context pack（小上下文包），不会把 raw events 大段塞回上下文。
2. Heartbeat reminder 已实现为 candidate / dry-run：只生成提醒候选，不真实发群，也不绕过 governance 自动写成 active 记忆。
3. Agent run summary candidate 已有 dry-run 形态，会记录任务、用过的 memory、缺口和新候选提示，但不会直接改记忆状态。
4. Reminder card 和 Bitable Reminder Candidates dry-run payload 已能展示 evidence、trigger、reason、due_at 和 recommended_action。
5. 历史后续的 2026-05-03 Benchmark Report 和指标自证已经完成；当前不要再按该日期计划继续执行，后续从 `docs/productization/full-copilot-next-execution-doc.md` 的产品化 Phase A 开始。

## 已完成总结

- `memory_engine/copilot/service.py`：新增 `prefetch`，内部复用 active-only search，返回 relevant memories、risks、deadlines、version status 和 trace summary。
- `memory_engine/copilot/tools.py`：`memory.prefetch` 已从“声明但未实现”改为调用 service；缺 scope 时返回统一 `scope_required`。
- `memory_engine/copilot/heartbeat.py`：新增 reminder candidate 引擎，覆盖 `review_due`、`important_not_recalled`、`deadline`、`thread_similarity` 四类触发源，并执行 importance、relevance、cooldown、scope permission、sensitive redaction 门控。
- `memory_engine/copilot/permissions.py`：新增 secret-like 文本脱敏，reminder dry-run 中会把明显 token / secret 替换成 `[REDACTED:...]`。
- `memory_engine/feishu_cards.py`：新增 Reminder Candidate typed payload 和 dry-run card builder，明确 `state_mutation=none`。
- `memory_engine/bitable_sync.py`：Reminder Candidates 表不再只是空字段设计，可接收 heartbeat 输出生成 dry-run rows。
- `memory_engine/benchmark.py`：新增 `copilot_prefetch` 和 `copilot_heartbeat` runner，输出 Agent Task Context Use Rate（任务前上下文使用率）、Sensitive Reminder Leakage Rate（敏感提醒泄漏率）等指标。
- `benchmarks/copilot_prefetch_cases.json`、`benchmarks/copilot_heartbeat_cases.json`：各新增 5 条可读样例，覆盖部署 checklist、截止时间、Demo 讲解、stale/superseded 过滤、Bitable dry-run、敏感文本脱敏。
- `agent_adapters/openclaw/examples/task_prefetch_flow.json`、`historical_decision_search.json`：示例 current_context 已改成工具可解析字段，并补充 expected_output。
- `tests/test_copilot_prefetch.py`、`tests/test_copilot_heartbeat.py`：新增专项单测；现有 tools/card/bitable 单测也补了 prefetch 和 reminder dry-run 覆盖。

## 验证结果

已运行：

```bash
python3 scripts/check_openclaw_version.py
git diff --check
python3 -m compileall memory_engine scripts
python3 -m unittest tests.test_copilot_prefetch tests.test_copilot_heartbeat
python3 -m unittest tests.test_copilot_tools tests.test_feishu_interactive_cards tests.test_bitable_sync
python3 -m memory_engine benchmark run benchmarks/copilot_prefetch_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_heartbeat_cases.json
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
ollama ps
```

当前已确认：

- OpenClaw version OK: 2026.4.24。
- `git diff --check` 通过。
- `compileall memory_engine scripts` 通过。
- prefetch / heartbeat 专项单测通过。
- tools / card / Bitable dry-run 相关单测通过。
- `copilot_prefetch_cases.json`：5 条样例，case_pass_rate = 1.0，agent_task_context_use_rate = 1.0，evidence_coverage = 1.0，stale_leakage_rate = 0.0。
- `copilot_heartbeat_cases.json`：5 条样例，case_pass_rate = 1.0，reminder_candidate_rate = 1.0，sensitive_reminder_leakage_rate = 0.0。
- `day1_cases.json` fallback：10 条样例，case_pass_rate = 1.0，conflict_accuracy = 1.0，stale_leakage_rate = 0.0。
- `ollama ps` 显示无本项目模型驻留；今天没有运行真实 Cognee / Ollama embedding 验证。

## 仍未完成或仍有风险

- `docs/benchmark-report.md` 还没有更新为 OpenClaw-native Copilot 的最终评委版，这是 2026-05-03 的主线。
- prefetch 目前证明了“任务前拿上下文”，但还没有把后续 agent output 是否引用 context pack 做成更强的端到端指标。
- heartbeat 目前只做 reminder candidate / card / Bitable dry-run，不做真实群推送，也不做复杂个性化节奏。
- sensitive redaction 覆盖明显 token / secret 模式；真实企业文本还需要后续用更多样例压测。
- Bitable 仍是 dry-run payload，不是 source of truth，也没有真实写生产表。

## 2026-05-03 接续建议

1. 历史建议原本指向 `docs/plans/2026-05-03-implementation-plan.md`；当前不要再执行该旧计划，先读 `docs/productization/full-copilot-next-execution-doc.md`。
2. 六类指标统一进 `docs/benchmark-report.md` 的历史后续任务已经完成；当前不要重复执行这条旧任务。
3. 重点补失败分类和 recommended fix，不要为了数字好看删掉边界样例。
4. 检查 Bitable Benchmark Results 字段是否能承载 2026-05-03 的统一指标。
5. 保留今天的 prefetch / heartbeat runner 结果作为报告证据。

## 我的补充任务

1. 已完成：按 `agent_adapters/openclaw/examples/` 补齐任务前 prefetch demo 样例，并让字段和 schema 对齐。
2. 已完成：记录 prefetch 输出应像“同事提前提醒”一样，包含 relevant memory、evidence、风险/截止时间和 trace 摘要。
3. 已完成：reminder 文案中的明显 token / secret 会脱敏，benchmark 验证 Sensitive Reminder Leakage Rate = 0。
4. 已完成：准备 Demo 讲解线索：先讲用户痛点，再讲 Agent 自动调用 `memory.prefetch` 和 heartbeat reminder candidate。
5. 已完成：Bitable dry-run 可展示 Reminder Candidates 表字段和一条 reminder candidate row。

本阶段不用做：

- 不用做复杂个性化推送。
- 不用把 reminder 直接发到真实群。
- 不用扩大 OpenClaw 工具范围。
- 不用把 Bitable 当 source of truth。
