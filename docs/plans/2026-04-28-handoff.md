# 2026-04-28 Handoff

> **历史状态更新（2026-04-28）**：本文件对应的 2026-05-05 及以前任务已经完成，保留为历史计划/交接证据，不再作为后续执行入口。新的执行入口是 `docs/productization/full-copilot-next-execution-doc.md`，下一步优先 Phase A：Storage Migration + Audit Table。

覆盖日期：2026-04-28
下一步日期：2026-04-29
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 今天已经把“查询历史决策”从简单 fallback 升级成能解释过程的 `memory.search`。
2. 现在 search trace 会显示 L0 / L1 / L2 / L3：L0 是当前任务上下文，L1 是常用热记忆，L2 是最近温记忆，L3 是历史冷证据。
3. `benchmarks/copilot_layer_cases.json` 已从 3 条补到 15 条，不改核心 Python 代码。
4. 15 条分层场景覆盖 Hot / Warm / Cold 各 5 条，每条都有中文备注说明为什么属于这一层。
5. 4 月 28 日稳定性硬化已完成：layer 指标、trace 契约、错误路径、evidence 底线和 benchmark fixture 自检都已补上。

## 已完成总结

- 新增 `memory_engine/copilot/retrieval.py`，提供 layer-aware search facade。今天不改旧 repository 大结构，先由 adapter 模拟 `layer`。
- 新增 `memory_engine/copilot/orchestrator.py`，按 L0 -> L1 -> L2 -> L3 -> merge -> rerank -> Top K 编排。
- 扩展 `memory_engine/copilot/schemas.py`，新增 `WorkingContext`、`MemoryLayer`、`RetrievalTrace`，并让 search result 带 `layer`。
- 调整 `memory_engine/copilot/service.py`，让 `CopilotService.search()` 走 orchestrator。
- 薄化 `memory_engine/copilot/tools.py`，工具层只解析请求、调用 service、包装错误。
- 新增 `tests/test_copilot_retrieval.py`，覆盖 trace、L1/L2 fallback、active-only、L3 raw event 不默认返回。
- 新增 `benchmarks/copilot_recall_cases.json` 和 `benchmarks/copilot_layer_cases.json` 最小草稿。
- 已把 `benchmarks/copilot_layer_cases.json` 扩展到 15 条：Hot 常用规则 5 条、Warm 近期任务 5 条、Cold 历史证据 5 条。
- 已复核 `benchmarks/copilot_recall_cases.json` 现有 5 条，问题形式像真实飞书项目群提问，并且每条都有正确答案和 evidence 关键词。
- 扩展 `memory_engine/benchmark.py`，让 `copilot_layer` 样例通过 `CopilotService.search()` 的 layer filter 路径验证 `actual_layer`，summary 输出 `layer_case_count` 和 `layer_accuracy`。
- 稳定 search trace contract：默认 search 保持 L0/L1/L2/L3 步骤可解释；Top K 满足后的未查询层会记录 `skipped_after_top_k_satisfied`；每步会说明 `layer_source` 和 `selection_reason`。
- 增加 evidence 缺失防线：缺 quote 的候选不会进入正式 Top results，trace 会记录 `dropped_missing_evidence`。
- 增加 `tests/test_copilot_benchmark.py`，固定 benchmark fixture 自检：case_id 唯一、Hot/Warm/Cold 各 5 条、每条有失败排查提示。
- 更新 `README.md` 顶部我的任务入口。

## 验证结果

```bash
python3 scripts/check_openclaw_version.py
python3 -m unittest tests.test_copilot_schemas tests.test_copilot_tools tests.test_copilot_retrieval
python3 -m unittest tests.test_copilot_schemas tests.test_copilot_tools tests.test_copilot_retrieval tests.test_copilot_cognee_adapter
python3 -m unittest discover tests
python3 -m compileall memory_engine scripts
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_recall_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_layer_cases.json
git diff --check
ollama ps
```

上述命令已通过；稳定性硬化后再次运行 `python3 -m memory_engine benchmark run benchmarks/copilot_layer_cases.json`，15 条样例全部通过，case pass rate = 1.0，layer accuracy = 1.0，evidence coverage = 1.0，stale leakage rate = 0.0。`ollama ps` 显示无本项目 embedding 模型驻留。

## 仍未完成或仍有风险

- `layer` 字段今天没有迁移到旧 `memories` 表；当前由 `LayerAwareRetriever` 根据当前请求路径模拟 L1/L2/L3。
- L3 现在只记录 trace 并阻止 raw events 直接进入默认答案，还没有实现真正的版本解释或深度证据检索。
- L1 p95 本地路径已有 trace 和毫秒级单测，但还不是正式 benchmark 指标。
- `expected_layer` 已进入 benchmark 指标，但这是通过显式 layer filter 验证 adapter 路径；后续如果要验证默认 cascade 自动判断 Hot/Warm/Cold，需要等 layer 持久字段或更强检索策略成型。

## PRD 完成度复核补充

2026-04-28 已新增 [PRD completion audit and gap tasks](../productization/prd-completion-audit-and-gap-tasks.md)，专门回答三个问题：MVP 是否完成、Feishu Memory Copilot 是否接入飞书、是否接入 OpenClaw 做完整产品形态测试。

当前复核口径：

- MVP 的 demo/pre-production 闭环已完成，可本地复现、可 benchmark、可 healthcheck。
- Feishu 已接入受控旧测试群 live sandbox，真实群消息进入 `memory_engine/copilot/feishu_live.py` -> `handle_tool_request()` -> `CopilotService`，但不是全量 Feishu workspace ingestion。
- OpenClaw 已完成 tool schema、examples、本地 bridge、demo replay 和受控产品形态测试，但还缺真实 OpenClaw Agent runtime 的独立验收记录和生产级长期运行。
- 未完成任务已拆成 5 条：storage/audit migration、真实 OpenClaw runtime 证据、Feishu staging runbook、live Cognee/Ollama embedding 验证、no-overclaim 交付物审查。

2026-04-28 已进一步新增 [完整可用 Copilot 后续执行文档](../productization/full-copilot-next-execution-doc.md)。后续目标不再是“初赛 MVP 已完成就停止”，而是按 Phase A-E 补齐完整可用 Copilot：storage/audit、真实 OpenClaw runtime、Feishu staging、live embedding gate 和 no-overclaim QA。下一步优先执行 Phase A：Storage Migration + Audit Table。

## 历史接续建议

1. 2026-04-29 的 hybrid retrieval 和 Cognee provenance 任务已经完成。
2. 本节只保留当时的交接思路：扩展 retrieval 时保留 trace 字段，例如 `layer_source`、`selection_reason`、`dropped_count`。
3. 当前不要再按 `docs/plans/2026-04-29-implementation-plan.md` 继续执行；后续从 `docs/productization/full-copilot-next-execution-doc.md` 的产品化 Phase A 开始。

## 我的补充任务

1. 已完成：`benchmarks/copilot_layer_cases.json` 已从 3 条扩到 15 条。
2. 已完成：每条都写了 `expected_layer` 和 `layer_reason`。Hot 是最常用记忆，Warm 是最近或待处理记忆，Cold 是历史或归档证据。
3. 已完成：`benchmarks/copilot_recall_cases.json` 的 5 条问题像真实飞书项目群提问，每条都有正确答案和 evidence 关键词。
4. 已完成：`python3 -m memory_engine benchmark run benchmarks/copilot_layer_cases.json` 通过，15/15 passed。
5. 已加固：每条 layer case 都有 `failure_debug_hint`；每条 recall case 都有 `expected_memory_intent` 和 `failure_debug_hint`。
