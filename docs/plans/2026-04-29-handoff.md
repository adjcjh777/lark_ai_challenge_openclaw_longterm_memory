# 2026-04-29 Handoff

覆盖日期：2026-04-29
下一步日期：2026-04-30
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 今天已经把 `memory.search` 从分层 fallback 升级成 hybrid retrieval（混合召回）：先筛选，再关键词，再本地向量信号，再可选 Cognee，最后合并排序。
2. 新增 `memory_engine/copilot/embeddings.py`，只对 curated memory 的 `type`、`subject`、`current_value`、`summary`、`evidence.quote` 做本地 deterministic embedding（可复现的轻量向量），不处理 raw events。
3. `RecallIndexEntry` 已落在 `memory_engine/copilot/retrieval.py`，短索引只保存可治理记忆字段，并用 evidence pointer 指回 Copilot ledger。
4. `benchmarks/copilot_recall_cases.json` 已扩到 8 条，覆盖 keyword-only、vector-only、stale-conflict，runner 现在能输出 Recall@3 和 Evidence Coverage（证据覆盖率）。
5. 2026-04-30 继续从 `docs/plans/2026-04-30-implementation-plan.md` 开始，重点是 candidate（待确认记忆）、confirm/reject 和 evidence gate，不要回头重做今天的混合召回。

## 已完成总结

- 新增 `RecallIndexEntry`，把 active curated memory 压成短索引文本，字段范围限定在 `type`、`subject`、`current_value`、`summary` 和 `evidence.quote`。
- 新增 deterministic embedding fallback，不引入新依赖，不调用真实 Ollama embedding，不向量化 raw events。
- `LayerAwareRetriever` 现在输出 hybrid trace，能看到 `structured`、`keyword`、`vector`、`cognee`、`rerank` 五类阶段。
- search result 新增 `matched_via` 和 `why_ranked`，可以解释来自 keyword_index、vector、Cognee 或 evidence/importance/recency 等排序信号。
- Cognee 仍是可选召回通道；未配置时 keyword/vector fallback 正常工作。Cognee 返回缺 provenance 且能匹配 Copilot ledger memory_id 时，会用 ledger 里的 evidence quote 补齐。
- 缺 evidence quote 的候选仍会被挡在正式结果外，不能进入 Top results。
- `memory_engine/benchmark.py` 已支持 `copilot_recall` 类型，输出 Recall@1、Recall@3、Evidence Coverage、stale leakage rate 和延迟指标。
- `benchmarks/copilot_recall_cases.json` 从 5 条扩到 8 条，补齐 `failure_category`，便于按 keyword_miss、vector_miss、wrong_subject_normalization、evidence_missing、stale_conflict 排查。
- README 顶部已切到 2026-04-30 下一步入口，并保留 2026-04-29 handoff 链接。

## 验证结果

```bash
python3 scripts/check_openclaw_version.py
python3 -m unittest tests.test_copilot_retrieval tests.test_copilot_cognee_adapter tests.test_copilot_benchmark
python3 -m compileall memory_engine scripts
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_recall_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_layer_cases.json
python3 -m unittest discover tests
ollama ps
```

结果：

- OpenClaw version OK: 2026.4.24。
- Copilot retrieval / Cognee adapter / benchmark 专项单测：18 tests OK。
- 全量单测：76 tests OK。
- `day1_cases.json`：case_pass_rate = 1.0，conflict_accuracy = 1.0，evidence_coverage = 1.0，stale_leakage_rate = 0.0。
- `copilot_recall_cases.json`：8 条样例，case_pass_rate = 1.0，Recall@1 = 1.0，Recall@3 = 1.0，Evidence Coverage = 1.0，stale leakage rate = 0.0。
- `copilot_layer_cases.json`：15 条样例，case_pass_rate = 1.0，layer_accuracy = 1.0，evidence_coverage = 1.0。
- `ollama ps` 显示无本项目模型驻留；今天没有运行真实 Cognee / Ollama embedding 验证。

## 仍未完成或仍有风险

- `layer` 仍由 adapter 模拟，没有迁移到旧 `memories` 表；今天的重点是 hybrid signal，不是 schema migration。
- 本地 deterministic embedding 只是 MVP fallback，用来证明 curated memory vector path 和测试可复现；真实 qwen3 embedding 仍走后续 Cognee/Ollama 验证路径。
- Cognee 目前只作为可选 recall/search 通道接入，未在默认本地 benchmark 中启动真实 Cognee SDK。
- `copilot_recall_cases.json` 目前 8 条已经可测，但还不是最终提交规模；后续 2026-05-03 会继续扩展评测报告。

## 2026-04-30 接续建议

1. 先读 `docs/plans/2026-04-30-implementation-plan.md`。
2. 从 `memory_engine/copilot/schemas.py`、`service.py`、`tools.py` 继续实现 `memory.create_candidate`、`memory.confirm`、`memory.reject`。
3. 保留今天的 evidence gate：没有 evidence quote 的候选不要变成 active。
4. 如果新增 candidate benchmark，先做最小 runner 或单测替代路径，再把命令写回计划。

## 我的补充任务

1. 已完成：人工检查 recall 样例，问题形式贴近真实飞书项目群提问。
2. 已完成：按 `keyword_miss`、`vector_miss`、`wrong_subject_normalization`、`evidence_missing`、`stale_conflict` 标注失败分类。
3. 已完成：把“应该命中哪条记忆”的说明补进 benchmark case 的 `expected_memory_intent` / `failure_debug_hint` / `note`。
4. 已完成：runner 输出 Top 3、trace、case_id、query、expected evidence，可用于失败复盘。
5. 已完成：新增 keyword-only、vector-only、stale-conflict 三类样例，并通过 `copilot_recall_cases.json` benchmark。
