# 2026-04-30 Handoff

> **历史状态更新（2026-04-28）**：本文件对应的 2026-05-05 及以前任务已经完成，保留为历史计划/交接证据，不再作为后续执行入口。新的执行入口是 `docs/productization/full-copilot-next-execution-doc.md`，下一步优先 Phase A：Storage Migration + Audit Table。

覆盖日期：2026-04-30
下一步日期：2026-05-01
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 今天已经把候选记忆治理入口接到 Copilot Core：`memory.create_candidate` 只生成 candidate（待确认记忆），不会默认进入 search。
2. `memory.confirm` 才会把 candidate 升级为 active；`memory.reject` 会把 candidate 留在 rejected，不参与默认召回。
3. 文档 ingestion 现在只作为 candidate source adapter：它把文档片段交给 `CopilotService.create_candidate()`，不再直接决定 active 状态。
4. 新增 30 条 candidate benchmark 样例，覆盖 15 条应该记、15 条不应该记，并输出 Candidate Precision（候选识别准确率）和失败分类。
5. 历史后续的 2026-05-01 冲突更新、版本解释、card/Bitable dry-run 任务已经完成；当前不要再按该日期计划继续执行，后续从 `docs/productization/full-copilot-next-execution-doc.md` 的产品化 Phase A 开始。

## 已完成总结

- 新增 `memory_engine/copilot/governance.py`，集中处理 candidate -> active / rejected 的状态转移、evidence gate、敏感内容门控和冲突候选。
- `memory.create_candidate` 已实现：低价值闲聊返回 `ignored`，有长期记忆信号的内容进入 candidate，高风险敏感内容不会 auto-confirm。
- `memory.confirm` 已实现：缺 evidence quote 的 candidate 不能升级为 active；确认后默认 search 才能召回。
- `memory.reject` 已实现：拒绝后不进入默认 recall。
- 同 subject 冲突候选已能生成 conflict object；confirm 冲突候选时 old active 会进入 superseded，新值成为 active。历史后续的版本解释和 benchmark 已完成。
- `document_ingestion.py` 已改为通过 Copilot service 生成 candidate，仍保留文档标题、token 和 quote 作为 evidence。
- 新增 `benchmarks/copilot_candidate_cases.json`，30 条样例每条都有原文、正确处理和白话原因。
- `memory_engine/benchmark.py` 已支持 `copilot_candidate` 类型，输出 `candidate_precision`、`candidate_not_detected`、`false_positive_candidate`、`evidence_missing`。
- README 顶部后来已切到完整产品化执行入口；本 handoff 只保留为 2026-04-30 历史证据。

## 验证结果

```bash
python3 scripts/check_openclaw_version.py
git diff --check
python3 -m unittest tests.test_copilot_governance tests.test_copilot_tools tests.test_document_ingestion tests.test_copilot_benchmark
python3 -m unittest tests.test_copilot_schemas tests.test_copilot_tools tests.test_copilot_retrieval tests.test_copilot_cognee_adapter tests.test_copilot_governance tests.test_copilot_benchmark tests.test_document_ingestion
python3 -m unittest discover tests
python3 -m compileall memory_engine scripts
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_candidate_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_recall_cases.json
python3 -m memory_engine benchmark run benchmarks/copilot_layer_cases.json
ollama ps
```

结果：

- OpenClaw version OK: 2026.4.24。
- governance / tools / document ingestion / benchmark 第一轮专项单测：30 tests OK。
- Copilot 相关扩展单测：63 tests OK。
- 全量单测：88 tests OK。
- `day1_cases.json`：case_pass_rate = 1.0，conflict_accuracy = 1.0，evidence_coverage = 1.0，stale_leakage_rate = 0.0。
- `copilot_candidate_cases.json`：30 条样例，case_pass_rate = 1.0，Candidate Precision = 1.0，candidate_not_detected = 0，false_positive_candidate = 0，evidence_missing = 0。
- `copilot_recall_cases.json`：8 条样例，case_pass_rate = 1.0，Recall@1 = 1.0，Recall@3 = 1.0，Evidence Coverage = 1.0，stale leakage rate = 0.0。
- `copilot_layer_cases.json`：15 条样例，case_pass_rate = 1.0，layer_accuracy = 1.0，evidence_coverage = 1.0。
- `ollama ps` 显示无本项目模型驻留；今天没有运行真实 Cognee / Ollama embedding 验证。

## 仍未完成或仍有风险

- `memory.explain_versions` 还没有实现；2026-05-01 要把今天的冲突候选和 superseded 版本链变成可解释输出。
- Card / Bitable review surface 还没有设计 typed payload；今天只做 Copilot service 输出，不接真实 Bitable 审核流。
- 冲突候选 benchmark 还没有独立成 `copilot_conflict_cases.json`；今天只在 governance 单测和 candidate benchmark 里覆盖最小冲突路径。
- sensitive gate 目前是最小规则，用于阻止明显 secret / token 自动确认；不是完整 DLP。
- subject 归一化当时仍沿用旧 extractor 的最小规则；历史后续已在 conflict/version 和 benchmark 切片中继续硬化，当前若再改应服务产品化 Phase A-E。

## 2026-05-01 接续建议

1. 历史建议原本指向 `docs/plans/2026-05-01-implementation-plan.md`；当前不要再执行该旧计划，先读 `docs/productization/full-copilot-next-execution-doc.md`。
2. old -> new 冲突更新的版本解释已在历史后续任务中完成；当前不要重复执行这条旧任务。
3. 在 `service.py` / `tools.py` 实现 `memory.explain_versions`。
4. 再去 `memory_engine/feishu_cards.py` 和 `memory_engine/bitable_sync.py` 设计 review surface typed payload，不要让 card/Bitable 直接改状态。
5. 新建 `benchmarks/copilot_conflict_cases.json`，把今天的最小冲突路径扩成可统计的 Conflict Update Accuracy。

## 我的补充任务

1. 已完成：补 30 条候选识别样例，15 条应该记，15 条不应该记。
2. 已完成：每条样例写了白话原因，说明“为什么值得记 / 为什么不值得记”。
3. 已完成：`memory.create_candidate` 输出里明确 `status=candidate`、`recommended_action` 和 `risk_flags`，能看出这是待确认记忆，不是已经生效。
4. 已完成：candidate benchmark 输出 `case_id`、原文、正确处理、失败分类和 response，便于复盘。
5. 已完成：遇到的高风险样例用 sensitive risk flag 保留人工复核，不做 auto-confirm。
