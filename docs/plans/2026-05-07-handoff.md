# 2026-05-07 Handoff

阶段：完整产品 Phase 1 Contract Freeze 已完成；Phase 2 权限前置实现已完成；Phase 2 OpenClaw live bridge 已完成；Phase 3 Feishu UI / Review Surface 已完成本地闭环；Phase 4 Limited Feishu ingestion 已完成本地闭环；Phase 5 Heartbeat Controlled Reminder 已完成本地闭环。

## 先看这个

1. 今天已经完成六段产品化推进：Phase 1 契约冻结、Phase 2 权限前置、Phase 2 OpenClaw live bridge、Phase 3 Feishu UI / Review Surface、Phase 4 Limited Feishu ingestion、Phase 5 Heartbeat Controlled Reminder。
2. 接下来不要重复做契约冻结、权限前置、OpenClaw bridge、review surface、Phase 4 candidate-only 基线或 Phase 5 reminder candidate 基线；下一步应从 Phase 6 healthcheck/deployability、Phase 4 真实 lark-cli 权限联调证据，或 Phase 5 更细的 source revoked/redaction 开始。
3. Phase 5 已交付的是：active memory 只生成 reminder candidate（提醒候选），带 reason、evidence、target actor、cooldown 和 permission trace，不自动 active，不真实推送飞书群消息。
4. 判断做对：缺失/畸形 `current_context.permission` fail closed；non-reviewer 看到敏感 reminder 时只拿到 withheld/redacted payload；card/Bitable dry-run 不展示未授权 evidence/current_value；Sensitive Reminder Leakage Rate = 0。
5. 遇到问题记录：lark-cli/Feishu 权限失败、source 权限撤销、candidate-only 证据不足、review surface 拒绝格式不稳定、误写成真实推送/生产调度/productized live。

## 已完成

### Phase 1 Contract Freeze

契约文档已经完成并作为后续事实源：

- [Storage Contract](../productization/contracts/storage-contract.md)
- [Permission Contract](../productization/contracts/permission-contract.md)
- [OpenClaw Payload Contract](../productization/contracts/openclaw-payload-contract.md)
- [Audit & Observability Contract](../productization/contracts/audit-observability-contract.md)
- [Migration RFC](../productization/contracts/migration-rfc.md)
- [Negative Permission Test Plan](../productization/contracts/negative-permission-test-plan.md)

提交：`a81be0c Freeze product storage and permission contracts`

### Phase 2 权限前置实现

本轮实现了 permission contract 的第一批代码落地：

- 新增 [tests/test_copilot_permissions.py](../../tests/test_copilot_permissions.py)。
- 更新 [memory_engine/copilot/schemas.py](../../memory_engine/copilot/schemas.py)，新增 `PermissionActor`、`PermissionSourceContext`、`PermissionContext`，并让 confirm/reject/explain_versions 携带 `current_context`。
- 更新 [memory_engine/copilot/permissions.py](../../memory_engine/copilot/permissions.py)，统一 `check_scope_access()`，覆盖 missing/malformed permission、action mismatch、scope/source mismatch、tenant/org mismatch、private visibility 和 reviewer role。
- 更新 [memory_engine/copilot/service.py](../../memory_engine/copilot/service.py)，让 `search/create_candidate/confirm/reject/explain_versions/prefetch` 全部经过统一权限门控。
- 更新 [memory_engine/copilot/governance.py](../../memory_engine/copilot/governance.py)，避免 `auto_confirm=True` 绕过 reviewer / owner / admin。
- 更新 [memory_engine/document_ingestion.py](../../memory_engine/document_ingestion.py)，真实 Feishu document ingestion 在 `lark-cli docs +fetch` 前先做权限校验；本地 Markdown fixture 仍可用 demo 权限跑旧测试。
- 更新 [agent_adapters/openclaw/memory_tools.schema.json](../../agent_adapters/openclaw/memory_tools.schema.json) 和 OpenClaw examples，要求 `current_context.permission`。

提交：`b6b17b4 Fail closed before Copilot permission-gated memory actions`

### Phase 2 OpenClaw live bridge

本轮已经完成 OpenClaw/本地桥到 permission-aware `CopilotService` 的统一入口：

- 更新 [memory_engine/copilot/tools.py](../../memory_engine/copilot/tools.py)，让 `memory.search/create_candidate/confirm/reject/explain_versions/prefetch` 通过 `handle_tool_request()` 调用 `CopilotService`。
- 输出统一携带 `bridge.request_id`、`bridge.trace_id` 和 `bridge.permission_decision`。
- `permission_denied` 仍保持稳定错误格式，并保留 `reason_code`。
- 更新 [agent_adapters/openclaw/memory_tools.schema.json](../../agent_adapters/openclaw/memory_tools.schema.json) 和 examples，标注 bridge envelope。

提交：`cb21bc7 Bridge OpenClaw tools through permission-aware Copilot service`

### Phase 3 Feishu UI / Review Surface

本轮已经把 review surface 切到 permission-aware service/tool output：

- 更新 [tests/test_feishu_interactive_cards.py](../../tests/test_feishu_interactive_cards.py)，先锁住 Candidate Review card、Version Chain card 和 card action 的权限拒绝行为。
- 更新 [tests/test_bitable_sync.py](../../tests/test_bitable_sync.py)，先锁住 Bitable Candidate Review dry-run 只消费 tool output，并带上 `request_id`、`trace_id`、`permission_decision`。
- 更新 [memory_engine/feishu_cards.py](../../memory_engine/feishu_cards.py)，permission denied 时只输出安全摘要，不展示未授权 `current_value` 或 evidence quote。
- 更新 [memory_engine/bitable_sync.py](../../memory_engine/bitable_sync.py)，Candidate Review 表行改为从 permission-aware tool/service output 生成，不再把 Bitable 当事实源。
- 更新 [memory_engine/feishu_runtime.py](../../memory_engine/feishu_runtime.py)，Feishu confirm/reject review action 走 `CopilotService` / `handle_tool_request`，不直接调用 repository 改 candidate 状态。

本阶段仍不是 Feishu live ingestion：没有拉真实飞书文档生成 candidate，没有自动 active 真实飞书数据，也没有做 audit table migration。

### Phase 4 Limited Feishu ingestion

本轮已经把指定 Feishu source -> candidate-only pipeline 的本地闭环补齐：

- 更新 [tests/test_document_ingestion.py](../../tests/test_document_ingestion.py)，先锁住授权 Feishu document source 进入 candidate、candidate 不自动 active、source context document_id 不匹配在 fetch 前 fail closed。
- 更新 [tests/test_copilot_tools.py](../../tests/test_copilot_tools.py)，锁住 `document_feishu` / Feishu source 即使带 `auto_confirm=True` 也只能保持 candidate。
- 更新 [memory_engine/document_ingestion.py](../../memory_engine/document_ingestion.py)，真实 Feishu source 在 `lark-cli docs +fetch` 前先校验 permission 和指定 document_id；成功结果带 `ingestion_trace`、`request_id`、`trace_id`、permission decision 和 source metadata。
- 更新 [memory_engine/copilot/service.py](../../memory_engine/copilot/service.py)，对真实 Feishu source 忽略 `auto_confirm=True`，返回 `auto_confirm_ignored` 和 `candidate_only_reason=feishu_source_candidate_only`。
- 新增 [limited_feishu_ingestion_candidate_only.json](../../agent_adapters/openclaw/examples/limited_feishu_ingestion_candidate_only.json)，明确 Phase 4 只是 schema demo / candidate-only 示例，不是 productized live 或全量 workspace ingestion。

本阶段仍不是 productized live：没有做全量 Feishu workspace ingestion，没有自动 active 真实飞书数据，没有生产部署，也没有完整 audit table migration。

### Phase 5 Heartbeat Controlled Reminder

本轮已经把 heartbeat 从普通 dry-run 提升为受控 reminder candidate 闭环：

- 更新 [tests/test_copilot_heartbeat.py](../../tests/test_copilot_heartbeat.py)，先锁住 active memory -> reminder candidate、`heartbeat.review_due` 工具入口、缺失/畸形 permission fail closed、non-reviewer 敏感 reminder withheld、不会自动 active。
- 更新 [tests/test_copilot_tools.py](../../tests/test_copilot_tools.py) 和 [agent_adapters/openclaw/memory_tools.schema.json](../../agent_adapters/openclaw/memory_tools.schema.json)，新增 `heartbeat.review_due`，仍走 permission-aware `CopilotService` / `handle_tool_request`。
- 更新 [memory_engine/copilot/heartbeat.py](../../memory_engine/copilot/heartbeat.py)、[service.py](../../memory_engine/copilot/service.py)、[tools.py](../../memory_engine/copilot/tools.py)，reminder candidate 输出 `reason`、`evidence`、`target_actor`、`cooldown`、`permission_trace` 和 `state_mutation=none`。
- 更新 [memory_engine/feishu_cards.py](../../memory_engine/feishu_cards.py) 和 [memory_engine/bitable_sync.py](../../memory_engine/bitable_sync.py)，card/Bitable dry-run 展示受控字段；withheld/redacted reminder 不展示敏感 `current_value` 或 evidence quote。
- 更新 [benchmarks/copilot_heartbeat_cases.json](../../benchmarks/copilot_heartbeat_cases.json) 和 [memory_engine/benchmark.py](../../memory_engine/benchmark.py)，新增 non-reviewer 敏感提醒 withheld 样例，heartbeat 样例数从 6 条变为 7 条。

本阶段仍不是 productized live：没有真实飞书群推送，没有生产调度服务，没有复杂个性化推荐，没有完整 audit table migration，也没有自动把 reminder 变成 active memory。

## 验证结果

已运行并通过：

```bash
python3 scripts/check_openclaw_version.py
git diff --check
python3 -m compileall memory_engine scripts
python3 -m unittest tests.test_copilot_permissions tests.test_copilot_schemas tests.test_copilot_tools tests.test_document_ingestion tests.test_feishu_interactive_cards tests.test_bitable_sync
python3 -m unittest tests.test_copilot_heartbeat tests.test_copilot_tools tests.test_feishu_interactive_cards tests.test_bitable_sync tests.test_copilot_permissions
python3 -m unittest discover tests
python3 -m memory_engine benchmark run benchmarks/copilot_heartbeat_cases.json
ollama ps
```

结果：

- OpenClaw version OK：`2026.4.24`
- 专项测试：当前 Phase 4 覆盖权限、schema、tools、document ingestion、Feishu card 和 Bitable dry-run；Phase 5 覆盖 heartbeat、tools、Feishu card、Bitable dry-run 和权限反例。
- 全量测试：`python3 -m unittest discover tests` 通过，148 个测试 OK。
- Heartbeat Benchmark（评测脚本）：7 条样例通过，`reminder_candidate_rate=1.0`，`sensitive_reminder_leakage_rate=0.0`，新增 non-reviewer 敏感提醒 withheld 样例。
- 架构审查：architect verification `APPROVED`，必须修复项为无。
- 本轮没有运行 Cognee / embedding / Ollama 相关验证；`ollama ps` 已确认无本项目模型驻留。

## 飞书共享看板

此前已同步并读回确认：

- 任务描述：`2026-05-07 程俊豪：Phase 2 权限前置实现`
- 状态：`已完成`
- 完成情况-程俊豪：`true`
- 记录 ID：`recvhZOaKscNb0`
- 备注包含：commit `b6b17b4`、OpenClaw 版本、专项/全量测试、架构审查、Ollama 清理状态和剩余风险。

本轮完成后需要继续同步一条程俊豪任务记录：

- 任务描述：`2026-05-07 程俊豪：Phase 5 Heartbeat Controlled Reminder`
- 状态：`已完成`
- 备注应包含：Phase 5 修改文件、验证命令、提交 hash、仍不是真实推送 / 生产调度 / productized live、Ollama 未拉起或已清理状态。

## 还没做

- Storage migration 还没实现：`tenant_id`、`organization_id`、`visibility_policy` 的数据库迁移仍在后续阶段。
- Audit table / healthcheck 还没实现。
- Phase 4 只完成指定 Feishu source 的本地 candidate-only 基线；如果 lark-cli/Feishu 权限失败，只能保持 fixture/dry-run 口径，不能声称真实空间 limited ingestion 已验证。
- Phase 5 只完成受控 reminder candidate；还没做真实推送、生产调度服务、复杂个性化推荐、source revoked 后 stale/redaction 处理。
- 还没做全量 Feishu workspace ingestion、生产部署。

## 下一步从哪里开始

建议下一轮从这三个文件开始：

1. [memory_engine/copilot/heartbeat.py](../../memory_engine/copilot/heartbeat.py)：如果继续打磨 Phase 5，下一步补 source revoked / stale / redaction 行为，仍保持 candidate-only。
2. [scripts/check_openclaw_version.py](../../scripts/check_openclaw_version.py) 和后续 healthcheck 文件：进入 Phase 6 时，从 deployability + healthcheck 开始。
3. [tests/test_document_ingestion.py](../../tests/test_document_ingestion.py)：如果继续打磨 Phase 4，下一步补真实 lark-cli 权限联调证据或 revoked source 行为。

下一轮标题建议：

```text
Phase 6 Deployability + Healthcheck：只做可检查性，不做生产部署
```
