# 2026-04-27 Handoff

覆盖日期：2026-04-26 至 2026-04-27
下一步日期：2026-04-28
主控：`docs/feishu-memory-copilot-implementation-plan.md`

## 执行前先看这个

1. 26/27 两天已经把新主线从“计划”推进到“可导入、可测试、可 fallback”的 Copilot 骨架。
2. OpenClaw 工具契约已经先冻结，后续不要再从旧 Feishu Bot handler 当主入口。
3. Cognee 本地 spike 已经跑通真实 `add -> cognify -> search` 闭环，但业务逻辑仍必须通过 `memory_engine/copilot/cognee_adapter.py` 窄边界接入。
4. 28 日从 `memory.search` 的 L0/L1/L2/L3 query cascade 开始，不要重新做依赖选型。
5. 遇到问题发我：失败命令、错误输出、涉及文件路径，以及是否影响 `memory.search` 的 active memory fallback。

## 已完成总结

### 2026-04-26

- 完成主控切换：新主控文档为 `docs/feishu-memory-copilot-implementation-plan.md`。
- 完成旧文档归档和绝对日期计划体系：`docs/plans/YYYY-MM-DD-implementation-plan.md`。
- 固定 OpenClaw 版本为 `2026.4.24`，新增 `agent_adapters/openclaw/openclaw-version.lock` 和 `scripts/check_openclaw_version.py`。
- 新增 OpenClaw 工具契约：`agent_adapters/openclaw/memory_tools.schema.json`。
- 新增 OpenClaw skill 和 examples：`agent_adapters/openclaw/feishu_memory_copilot.skill.md`、`agent_adapters/openclaw/examples/*.json`。
- 新增 Copilot skeleton：`memory_engine/copilot/__init__.py`、`schemas.py`、`tools.py`、`cognee_adapter.py`。
- 新增第一批 Copilot 测试：`tests/test_copilot_schemas.py`、`tests/test_copilot_tools.py`。

### 2026-04-27

- 锁定 Python 依赖：`cognee==0.1.20`、`httpx==0.27.2`。
- 新增 Cognee 本地 spike：`scripts/spike_cognee_local.py`，支持 `--dry-run` 和 `--reset-local-data`。
- 新增 Cognee adapter contract 测试：`tests/test_copilot_cognee_adapter.py`。
- 新增 `memory_engine/copilot/service.py` 和 `permissions.py`，`memory.search` 已能通过旧 `MemoryRepository` fallback 返回 active memory with evidence。
- 验证 RightCode 文本模型通路可用：`gpt-5.3-codex-high` 最小 chat completion 可返回结果。
- 确认 RightCode embedding 不可用：`/embeddings` 对 `text-embedding-3-large` 返回 `PermissionDeniedError`。
- 选择本地 Ollama embedding，锁定 `qwen3-embedding:0.6b-fp16` 到 `memory_engine/copilot/embedding-provider.lock`。
- 新增 embedding 检查和本地复现 setup：`scripts/check_embedding_provider.py`、`scripts/setup_embedding_ollama_macos.sh`、`scripts/setup_embedding_ollama_windows.ps1`、`docs/reference/local-windows-cognee-embedding-setup.md`。
- 清理旧 `.data/cognee/` 3072 维残留后，已验证 Cognee 真实 `add -> cognify -> search` 三阶段闭环。

## 当前可验证命令

```bash
python3 scripts/check_openclaw_version.py
python3 -m unittest tests.test_copilot_schemas tests.test_copilot_tools tests.test_copilot_cognee_adapter
python3 scripts/check_embedding_provider.py
python3 -m compileall memory_engine scripts
python3 -m memory_engine benchmark run benchmarks/day1_cases.json
```

运行 `python3 scripts/check_embedding_provider.py` 后要检查并清理本项目 Ollama 模型：

```bash
ollama ps
ollama stop qwen3-embedding:0.6b-fp16
```

## 仍未完成

- L0/L1/L2/L3 query cascade 尚未实现。
- `memory_engine/copilot/retrieval.py`、`orchestrator.py`、`embeddings.py` 尚未进入完整实现阶段。
- `memory.create_candidate`、`memory.confirm`、`memory.reject`、`memory.explain_versions`、`memory.prefetch` 只完成 schema / request contract，尚未实现业务闭环。
- `benchmarks/copilot_recall_cases.json`、`copilot_layer_cases.json` 等新 benchmark 文件尚未创建。
- README 已更新为当前主线入口，但 `docs/demo-runbook.md` 和 `docs/benchmark-report.md` 仍主要服务旧 demo / 旧 benchmark，按 2026-05-03 至 2026-05-04 计划再系统更新。

后续状态提示：上述是 2026-04-27 交接时的缺口。当前仓库已经在 2026-04-28 切片中补上 `orchestrator.py`、`retrieval.py`、L0/L1/L2/L3 trace、`copilot_recall_cases.json` 和 15 条 `copilot_layer_cases.json`，详见 `docs/plans/2026-04-28-handoff.md`。仍未闭合的是 `memory.create_candidate`、`memory.confirm`、`memory.reject`、`memory.explain_versions`、`memory.prefetch`，这些继续按 2026-04-30 至 2026-05-02 的日期计划推进。

## 2026-04-28 接续建议

1. 先读 `docs/plans/2026-04-28-implementation-plan.md` 和本 handoff。
2. 从 `memory_engine/copilot/schemas.py` 补 `WorkingContext`、`MemoryLayer`、`RetrievalTrace`。
3. 新增 `memory_engine/copilot/orchestrator.py` 和 `retrieval.py`，实现 L0 -> L1 -> L2 -> L3 -> merge -> rerank -> Top K 的最小 trace。
4. 保持 `tools.py` 是薄封装，核心仍在 `CopilotService.search()`。
5. 默认 search 只返回 active；superseded、archived、raw events 不作为当前答案。

## 我的补充任务

1. 准备 `benchmarks/copilot_recall_cases.json` 的 10 条真实项目问题草稿，每条写 query、expected、evidence_keyword。
2. 准备 `benchmarks/copilot_layer_cases.json` 的 15 条分层样例，标注 Hot / Warm / Cold 的白话原因。
3. 检查 `docs/reference/local-windows-cognee-embedding-setup.md` 是否能支持 Windows 备用环境独立安装 Ollama 和拉取模型。
4. 检查 `agent_adapters/openclaw/examples/*.json` 的字段是否能看懂，不清楚的字段直接标注到后续 handoff。
5. 这组补充任务不改核心 Python 代码，也不用接真实飞书权限。
